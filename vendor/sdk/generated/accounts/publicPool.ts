/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getPoolStateDecoder,
  getPoolStateEncoder,
  type PoolState,
  type PoolStateArgs,
} from "../types";

export const PUBLIC_POOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  85, 93, 63, 121, 66, 154, 130, 37,
]);

export function getPublicPoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PUBLIC_POOL_DISCRIMINATOR);
}

export type PublicPool = {
  discriminator: ReadonlyUint8Array;
  poolType: number;
  roundNumber: bigint;
  /** Set to `Clock::get()?.unix_timestamp` at the moment of init/reopen. */
  openTime: bigint;
  /** Enforced by the Solana clock sysvar at draw time, not by user input. */
  closeTime: bigint;
  /**
   * Locked at init for the lifetime of this round; equals
   * `TICKET_PRICE_LAMPORTS` for v1 public pools but stored to allow private
   * pools to vary in step 8.
   */
  ticketPrice: bigint;
  totalTickets: bigint;
  /** Sum of all ticket purchases, in lamports. Excludes any rent on PDAs. */
  totalPot: bigint;
  state: PoolState;
  winningTicket: Option<bigint>;
  winner: Option<Address>;
  /**
   * The Switchboard On-Demand `RandomnessAccountData` PDA bound to this
   * round's draw. Set when `commit_draw_public` runs; read at settle time
   * to verify the same account flows through both halves of the commit-
   * reveal handshake. Spec calls this `vrf_request` (line 134) — kept the
   * name for spec-fidelity even though "randomness account" is the
   * On-Demand term.
   */
  vrfRequest: Option<Address>;
  /**
   * Slot at which `commit_draw_public` ran. Settle requires the randomness
   * account's `seed_slot` and current `clock.slot` to satisfy Switchboard's
   * freshness check; storing this lets us reason about retries.
   */
  commitSlot: bigint;
  /**
   * Lamports actually refunded from the pool to the commit caller (capped
   * at `VRF_COST_LAMPORTS`). Subtracted from the protocol fee at settle so
   * the treasury share is `protocol_fee - vrf_paid`.
   */
  vrfPaid: bigint;
  bump: number;
};

export type PublicPoolArgs = {
  poolType: number;
  roundNumber: number | bigint;
  /** Set to `Clock::get()?.unix_timestamp` at the moment of init/reopen. */
  openTime: number | bigint;
  /** Enforced by the Solana clock sysvar at draw time, not by user input. */
  closeTime: number | bigint;
  /**
   * Locked at init for the lifetime of this round; equals
   * `TICKET_PRICE_LAMPORTS` for v1 public pools but stored to allow private
   * pools to vary in step 8.
   */
  ticketPrice: number | bigint;
  totalTickets: number | bigint;
  /** Sum of all ticket purchases, in lamports. Excludes any rent on PDAs. */
  totalPot: number | bigint;
  state: PoolStateArgs;
  winningTicket: OptionOrNullable<number | bigint>;
  winner: OptionOrNullable<Address>;
  /**
   * The Switchboard On-Demand `RandomnessAccountData` PDA bound to this
   * round's draw. Set when `commit_draw_public` runs; read at settle time
   * to verify the same account flows through both halves of the commit-
   * reveal handshake. Spec calls this `vrf_request` (line 134) — kept the
   * name for spec-fidelity even though "randomness account" is the
   * On-Demand term.
   */
  vrfRequest: OptionOrNullable<Address>;
  /**
   * Slot at which `commit_draw_public` ran. Settle requires the randomness
   * account's `seed_slot` and current `clock.slot` to satisfy Switchboard's
   * freshness check; storing this lets us reason about retries.
   */
  commitSlot: number | bigint;
  /**
   * Lamports actually refunded from the pool to the commit caller (capped
   * at `VRF_COST_LAMPORTS`). Subtracted from the protocol fee at settle so
   * the treasury share is `protocol_fee - vrf_paid`.
   */
  vrfPaid: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link PublicPoolArgs} account data. */
export function getPublicPoolEncoder(): Encoder<PublicPoolArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["poolType", getU8Encoder()],
      ["roundNumber", getU64Encoder()],
      ["openTime", getI64Encoder()],
      ["closeTime", getI64Encoder()],
      ["ticketPrice", getU64Encoder()],
      ["totalTickets", getU64Encoder()],
      ["totalPot", getU64Encoder()],
      ["state", getPoolStateEncoder()],
      ["winningTicket", getOptionEncoder(getU64Encoder())],
      ["winner", getOptionEncoder(getAddressEncoder())],
      ["vrfRequest", getOptionEncoder(getAddressEncoder())],
      ["commitSlot", getU64Encoder()],
      ["vrfPaid", getU64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PUBLIC_POOL_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link PublicPool} account data. */
export function getPublicPoolDecoder(): Decoder<PublicPool> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["poolType", getU8Decoder()],
    ["roundNumber", getU64Decoder()],
    ["openTime", getI64Decoder()],
    ["closeTime", getI64Decoder()],
    ["ticketPrice", getU64Decoder()],
    ["totalTickets", getU64Decoder()],
    ["totalPot", getU64Decoder()],
    ["state", getPoolStateDecoder()],
    ["winningTicket", getOptionDecoder(getU64Decoder())],
    ["winner", getOptionDecoder(getAddressDecoder())],
    ["vrfRequest", getOptionDecoder(getAddressDecoder())],
    ["commitSlot", getU64Decoder()],
    ["vrfPaid", getU64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link PublicPool} account data. */
export function getPublicPoolCodec(): Codec<PublicPoolArgs, PublicPool> {
  return combineCodec(getPublicPoolEncoder(), getPublicPoolDecoder());
}

export function decodePublicPool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<PublicPool, TAddress>;
export function decodePublicPool<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<PublicPool, TAddress>;
export function decodePublicPool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<PublicPool, TAddress> | MaybeAccount<PublicPool, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPublicPoolDecoder(),
  );
}

export async function fetchPublicPool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<PublicPool, TAddress>> {
  const maybeAccount = await fetchMaybePublicPool(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePublicPool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<PublicPool, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePublicPool(maybeAccount);
}

export async function fetchAllPublicPool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<PublicPool>[]> {
  const maybeAccounts = await fetchAllMaybePublicPool(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePublicPool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<PublicPool>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodePublicPool(maybeAccount));
}
