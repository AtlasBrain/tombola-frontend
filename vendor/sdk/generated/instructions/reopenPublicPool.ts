/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";

export const REOPEN_PUBLIC_POOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([64, 179, 27, 46, 107, 237, 46, 201]);

export function getReopenPublicPoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REOPEN_PUBLIC_POOL_DISCRIMINATOR,
  );
}

export type ReopenPublicPoolInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountCounter extends string | AccountMeta<string> = string,
  TAccountPreviousPool extends string | AccountMeta<string> = string,
  TAccountNewPool extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCounter extends string
        ? WritableAccount<TAccountCounter>
        : TAccountCounter,
      TAccountPreviousPool extends string
        ? ReadonlyAccount<TAccountPreviousPool>
        : TAccountPreviousPool,
      TAccountNewPool extends string
        ? WritableAccount<TAccountNewPool>
        : TAccountNewPool,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ReopenPublicPoolInstructionData = {
  discriminator: ReadonlyUint8Array;
  poolType: number;
};

export type ReopenPublicPoolInstructionDataArgs = { poolType: number };

export function getReopenPublicPoolInstructionDataEncoder(): FixedSizeEncoder<ReopenPublicPoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["poolType", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REOPEN_PUBLIC_POOL_DISCRIMINATOR }),
  );
}

export function getReopenPublicPoolInstructionDataDecoder(): FixedSizeDecoder<ReopenPublicPoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["poolType", getU8Decoder()],
  ]);
}

export function getReopenPublicPoolInstructionDataCodec(): FixedSizeCodec<
  ReopenPublicPoolInstructionDataArgs,
  ReopenPublicPoolInstructionData
> {
  return combineCodec(
    getReopenPublicPoolInstructionDataEncoder(),
    getReopenPublicPoolInstructionDataDecoder(),
  );
}

export type ReopenPublicPoolInput<
  TAccountCounter extends string = string,
  TAccountPreviousPool extends string = string,
  TAccountNewPool extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  counter: Address<TAccountCounter>;
  /**
   * The just-resolved round whose close_time + duration becomes the new
   * round's close_time.
   */
  previousPool: Address<TAccountPreviousPool>;
  /**
   * The new round's PDA. Round number = counter.current_round + 1 at
   * instruction-entry time.
   */
  newPool: Address<TAccountNewPool>;
  /** Permissionless. Pays rent for the new round PDA. */
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  poolType: ReopenPublicPoolInstructionDataArgs["poolType"];
};

export function getReopenPublicPoolInstruction<
  TAccountCounter extends string,
  TAccountPreviousPool extends string,
  TAccountNewPool extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: ReopenPublicPoolInput<
    TAccountCounter,
    TAccountPreviousPool,
    TAccountNewPool,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ReopenPublicPoolInstruction<
  TProgramAddress,
  TAccountCounter,
  TAccountPreviousPool,
  TAccountNewPool,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    counter: { value: input.counter ?? null, isWritable: true },
    previousPool: { value: input.previousPool ?? null, isWritable: false },
    newPool: { value: input.newPool ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("counter", accounts.counter),
      getAccountMeta("previousPool", accounts.previousPool),
      getAccountMeta("newPool", accounts.newPool),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getReopenPublicPoolInstructionDataEncoder().encode(
      args as ReopenPublicPoolInstructionDataArgs,
    ),
    programAddress,
  } as ReopenPublicPoolInstruction<
    TProgramAddress,
    TAccountCounter,
    TAccountPreviousPool,
    TAccountNewPool,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ParsedReopenPublicPoolInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    counter: TAccountMetas[0];
    /**
     * The just-resolved round whose close_time + duration becomes the new
     * round's close_time.
     */
    previousPool: TAccountMetas[1];
    /**
     * The new round's PDA. Round number = counter.current_round + 1 at
     * instruction-entry time.
     */
    newPool: TAccountMetas[2];
    /** Permissionless. Pays rent for the new round PDA. */
    payer: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: ReopenPublicPoolInstructionData;
};

export function parseReopenPublicPoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReopenPublicPoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      counter: getNextAccount(),
      previousPool: getNextAccount(),
      newPool: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getReopenPublicPoolInstructionDataDecoder().decode(instruction.data),
  };
}
