/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findWhitelistedPda } from "../pdas";
import { RAFFLE_V2_PROGRAM_ADDRESS } from "../programs";

export const BUY_TICKET_PRIVATE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([3, 86, 178, 115, 34, 11, 147, 51]);

export function getBuyTicketPrivateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    BUY_TICKET_PRIVATE_DISCRIMINATOR,
  );
}

export type BuyTicketPrivateInstruction<
  TProgram extends string = typeof RAFFLE_V2_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountWhitelisted extends string | AccountMeta<string> = string,
  TAccountTicketBatch extends string | AccountMeta<string> = string,
  TAccountBuyer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountWhitelisted extends string
        ? ReadonlyAccount<TAccountWhitelisted>
        : TAccountWhitelisted,
      TAccountTicketBatch extends string
        ? WritableAccount<TAccountTicketBatch>
        : TAccountTicketBatch,
      TAccountBuyer extends string
        ? WritableSignerAccount<TAccountBuyer> &
            AccountSignerMeta<TAccountBuyer>
        : TAccountBuyer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type BuyTicketPrivateInstructionData = {
  discriminator: ReadonlyUint8Array;
  quantity: bigint;
};

export type BuyTicketPrivateInstructionDataArgs = { quantity: number | bigint };

export function getBuyTicketPrivateInstructionDataEncoder(): FixedSizeEncoder<BuyTicketPrivateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["quantity", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BUY_TICKET_PRIVATE_DISCRIMINATOR }),
  );
}

export function getBuyTicketPrivateInstructionDataDecoder(): FixedSizeDecoder<BuyTicketPrivateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["quantity", getU64Decoder()],
  ]);
}

export function getBuyTicketPrivateInstructionDataCodec(): FixedSizeCodec<
  BuyTicketPrivateInstructionDataArgs,
  BuyTicketPrivateInstructionData
> {
  return combineCodec(
    getBuyTicketPrivateInstructionDataEncoder(),
    getBuyTicketPrivateInstructionDataDecoder(),
  );
}

export type BuyTicketPrivateAsyncInput<
  TAccountPool extends string = string,
  TAccountWhitelisted extends string = string,
  TAccountTicketBatch extends string = string,
  TAccountBuyer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pool: Address<TAccountPool>;
  /**
   * Eligibility proof from `redeem_invite_code_whitelist`. Existence of
   * this PDA is the gate; the address derivation alone enforces that
   * `buyer` was the redeemer.
   */
  whitelisted?: Address<TAccountWhitelisted>;
  /**
   * One TicketBatch per purchase, same convention as buy_ticket_public
   * (D-025). Seeded by `pool.total_tickets` at instruction-entry.
   */
  ticketBatch: Address<TAccountTicketBatch>;
  buyer: TransactionSigner<TAccountBuyer>;
  systemProgram?: Address<TAccountSystemProgram>;
  quantity: BuyTicketPrivateInstructionDataArgs["quantity"];
};

export async function getBuyTicketPrivateInstructionAsync<
  TAccountPool extends string,
  TAccountWhitelisted extends string,
  TAccountTicketBatch extends string,
  TAccountBuyer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_V2_PROGRAM_ADDRESS,
>(
  input: BuyTicketPrivateAsyncInput<
    TAccountPool,
    TAccountWhitelisted,
    TAccountTicketBatch,
    TAccountBuyer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  BuyTicketPrivateInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountWhitelisted,
    TAccountTicketBatch,
    TAccountBuyer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    whitelisted: { value: input.whitelisted ?? null, isWritable: false },
    ticketBatch: { value: input.ticketBatch ?? null, isWritable: true },
    buyer: { value: input.buyer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.whitelisted.value) {
    accounts.whitelisted.value = await findWhitelistedPda({
      pool: getAddressFromResolvedInstructionAccount(
        "pool",
        accounts.pool.value,
      ),
      buyer: getAddressFromResolvedInstructionAccount(
        "buyer",
        accounts.buyer.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("whitelisted", accounts.whitelisted),
      getAccountMeta("ticketBatch", accounts.ticketBatch),
      getAccountMeta("buyer", accounts.buyer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getBuyTicketPrivateInstructionDataEncoder().encode(
      args as BuyTicketPrivateInstructionDataArgs,
    ),
    programAddress,
  } as BuyTicketPrivateInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountWhitelisted,
    TAccountTicketBatch,
    TAccountBuyer,
    TAccountSystemProgram
  >);
}

export type BuyTicketPrivateInput<
  TAccountPool extends string = string,
  TAccountWhitelisted extends string = string,
  TAccountTicketBatch extends string = string,
  TAccountBuyer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pool: Address<TAccountPool>;
  /**
   * Eligibility proof from `redeem_invite_code_whitelist`. Existence of
   * this PDA is the gate; the address derivation alone enforces that
   * `buyer` was the redeemer.
   */
  whitelisted: Address<TAccountWhitelisted>;
  /**
   * One TicketBatch per purchase, same convention as buy_ticket_public
   * (D-025). Seeded by `pool.total_tickets` at instruction-entry.
   */
  ticketBatch: Address<TAccountTicketBatch>;
  buyer: TransactionSigner<TAccountBuyer>;
  systemProgram?: Address<TAccountSystemProgram>;
  quantity: BuyTicketPrivateInstructionDataArgs["quantity"];
};

export function getBuyTicketPrivateInstruction<
  TAccountPool extends string,
  TAccountWhitelisted extends string,
  TAccountTicketBatch extends string,
  TAccountBuyer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_V2_PROGRAM_ADDRESS,
>(
  input: BuyTicketPrivateInput<
    TAccountPool,
    TAccountWhitelisted,
    TAccountTicketBatch,
    TAccountBuyer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): BuyTicketPrivateInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountWhitelisted,
  TAccountTicketBatch,
  TAccountBuyer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    whitelisted: { value: input.whitelisted ?? null, isWritable: false },
    ticketBatch: { value: input.ticketBatch ?? null, isWritable: true },
    buyer: { value: input.buyer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("whitelisted", accounts.whitelisted),
      getAccountMeta("ticketBatch", accounts.ticketBatch),
      getAccountMeta("buyer", accounts.buyer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getBuyTicketPrivateInstructionDataEncoder().encode(
      args as BuyTicketPrivateInstructionDataArgs,
    ),
    programAddress,
  } as BuyTicketPrivateInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountWhitelisted,
    TAccountTicketBatch,
    TAccountBuyer,
    TAccountSystemProgram
  >);
}

export type ParsedBuyTicketPrivateInstruction<
  TProgram extends string = typeof RAFFLE_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
    /**
     * Eligibility proof from `redeem_invite_code_whitelist`. Existence of
     * this PDA is the gate; the address derivation alone enforces that
     * `buyer` was the redeemer.
     */
    whitelisted: TAccountMetas[1];
    /**
     * One TicketBatch per purchase, same convention as buy_ticket_public
     * (D-025). Seeded by `pool.total_tickets` at instruction-entry.
     */
    ticketBatch: TAccountMetas[2];
    buyer: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: BuyTicketPrivateInstructionData;
};

export function parseBuyTicketPrivateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedBuyTicketPrivateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      whitelisted: getNextAccount(),
      ticketBatch: getNextAccount(),
      buyer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getBuyTicketPrivateInstructionDataDecoder().decode(instruction.data),
  };
}
