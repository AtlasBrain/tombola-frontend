/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { RAFFLE_V2_PROGRAM_ADDRESS } from "../programs";

export const SETTLE_DRAW_PUBLIC_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([79, 87, 208, 81, 168, 138, 148, 27]);

export function getSettleDrawPublicDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SETTLE_DRAW_PUBLIC_DISCRIMINATOR,
  );
}

export type SettleDrawPublicInstruction<
  TProgram extends string = typeof RAFFLE_V2_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountWinningBatch extends string | AccountMeta<string> = string,
  TAccountWinner extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountRandomnessAccount extends string | AccountMeta<string> = string,
  TAccountCaller extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountWinningBatch extends string
        ? ReadonlyAccount<TAccountWinningBatch>
        : TAccountWinningBatch,
      TAccountWinner extends string
        ? WritableAccount<TAccountWinner>
        : TAccountWinner,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountRandomnessAccount extends string
        ? ReadonlyAccount<TAccountRandomnessAccount>
        : TAccountRandomnessAccount,
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      ...TRemainingAccounts,
    ]
  >;

export type SettleDrawPublicInstructionData = {
  discriminator: ReadonlyUint8Array;
  poolType: number;
  round: bigint;
};

export type SettleDrawPublicInstructionDataArgs = {
  poolType: number;
  round: number | bigint;
};

export function getSettleDrawPublicInstructionDataEncoder(): FixedSizeEncoder<SettleDrawPublicInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["poolType", getU8Encoder()],
      ["round", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SETTLE_DRAW_PUBLIC_DISCRIMINATOR }),
  );
}

export function getSettleDrawPublicInstructionDataDecoder(): FixedSizeDecoder<SettleDrawPublicInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["poolType", getU8Decoder()],
    ["round", getU64Decoder()],
  ]);
}

export function getSettleDrawPublicInstructionDataCodec(): FixedSizeCodec<
  SettleDrawPublicInstructionDataArgs,
  SettleDrawPublicInstructionData
> {
  return combineCodec(
    getSettleDrawPublicInstructionDataEncoder(),
    getSettleDrawPublicInstructionDataDecoder(),
  );
}

export type SettleDrawPublicAsyncInput<
  TAccountPool extends string = string,
  TAccountWinningBatch extends string = string,
  TAccountWinner extends string = string,
  TAccountConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountRandomnessAccount extends string = string,
  TAccountCaller extends string = string,
> = {
  pool: Address<TAccountPool>;
  /**
   * The TicketBatch claimed to contain the winning ticket. Must satisfy:
   * `batch.pool == pool.key()` and
   * `batch.first_ticket_id <= winner_id <= batch.last_ticket_id`.
   * Anchor's macro doesn't pre-derive the batch's PDA seed (we don't know
   * `first_ticket_id` here) — we validate the link manually in the
   * handler.
   */
  winningBatch: Address<TAccountWinningBatch>;
  /**
   * The winner's wallet — must equal `winning_batch.owner`. mut to receive
   * 99.5% of the pot.
   */
  winner: Address<TAccountWinner>;
  config?: Address<TAccountConfig>;
  /**
   * The treasury account from `ProtocolConfig.treasury`. mut to receive
   * `protocol_fee - vrf_paid`.
   */
  treasury: Address<TAccountTreasury>;
  /**
   * The Switchboard On-Demand `RandomnessAccountData` PDA, must match the
   * one stored at commit time (`pool.vrf_request`).
   */
  randomnessAccount: Address<TAccountRandomnessAccount>;
  /** Permissionless. Caller pays only the tx fee. */
  caller: TransactionSigner<TAccountCaller>;
  poolType: SettleDrawPublicInstructionDataArgs["poolType"];
  round: SettleDrawPublicInstructionDataArgs["round"];
};

export async function getSettleDrawPublicInstructionAsync<
  TAccountPool extends string,
  TAccountWinningBatch extends string,
  TAccountWinner extends string,
  TAccountConfig extends string,
  TAccountTreasury extends string,
  TAccountRandomnessAccount extends string,
  TAccountCaller extends string,
  TProgramAddress extends Address = typeof RAFFLE_V2_PROGRAM_ADDRESS,
>(
  input: SettleDrawPublicAsyncInput<
    TAccountPool,
    TAccountWinningBatch,
    TAccountWinner,
    TAccountConfig,
    TAccountTreasury,
    TAccountRandomnessAccount,
    TAccountCaller
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SettleDrawPublicInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountWinningBatch,
    TAccountWinner,
    TAccountConfig,
    TAccountTreasury,
    TAccountRandomnessAccount,
    TAccountCaller
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    winningBatch: { value: input.winningBatch ?? null, isWritable: false },
    winner: { value: input.winner ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    treasury: { value: input.treasury ?? null, isWritable: true },
    randomnessAccount: {
      value: input.randomnessAccount ?? null,
      isWritable: false,
    },
    caller: { value: input.caller ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("winningBatch", accounts.winningBatch),
      getAccountMeta("winner", accounts.winner),
      getAccountMeta("config", accounts.config),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("randomnessAccount", accounts.randomnessAccount),
      getAccountMeta("caller", accounts.caller),
    ],
    data: getSettleDrawPublicInstructionDataEncoder().encode(
      args as SettleDrawPublicInstructionDataArgs,
    ),
    programAddress,
  } as SettleDrawPublicInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountWinningBatch,
    TAccountWinner,
    TAccountConfig,
    TAccountTreasury,
    TAccountRandomnessAccount,
    TAccountCaller
  >);
}

export type SettleDrawPublicInput<
  TAccountPool extends string = string,
  TAccountWinningBatch extends string = string,
  TAccountWinner extends string = string,
  TAccountConfig extends string = string,
  TAccountTreasury extends string = string,
  TAccountRandomnessAccount extends string = string,
  TAccountCaller extends string = string,
> = {
  pool: Address<TAccountPool>;
  /**
   * The TicketBatch claimed to contain the winning ticket. Must satisfy:
   * `batch.pool == pool.key()` and
   * `batch.first_ticket_id <= winner_id <= batch.last_ticket_id`.
   * Anchor's macro doesn't pre-derive the batch's PDA seed (we don't know
   * `first_ticket_id` here) — we validate the link manually in the
   * handler.
   */
  winningBatch: Address<TAccountWinningBatch>;
  /**
   * The winner's wallet — must equal `winning_batch.owner`. mut to receive
   * 99.5% of the pot.
   */
  winner: Address<TAccountWinner>;
  config: Address<TAccountConfig>;
  /**
   * The treasury account from `ProtocolConfig.treasury`. mut to receive
   * `protocol_fee - vrf_paid`.
   */
  treasury: Address<TAccountTreasury>;
  /**
   * The Switchboard On-Demand `RandomnessAccountData` PDA, must match the
   * one stored at commit time (`pool.vrf_request`).
   */
  randomnessAccount: Address<TAccountRandomnessAccount>;
  /** Permissionless. Caller pays only the tx fee. */
  caller: TransactionSigner<TAccountCaller>;
  poolType: SettleDrawPublicInstructionDataArgs["poolType"];
  round: SettleDrawPublicInstructionDataArgs["round"];
};

export function getSettleDrawPublicInstruction<
  TAccountPool extends string,
  TAccountWinningBatch extends string,
  TAccountWinner extends string,
  TAccountConfig extends string,
  TAccountTreasury extends string,
  TAccountRandomnessAccount extends string,
  TAccountCaller extends string,
  TProgramAddress extends Address = typeof RAFFLE_V2_PROGRAM_ADDRESS,
>(
  input: SettleDrawPublicInput<
    TAccountPool,
    TAccountWinningBatch,
    TAccountWinner,
    TAccountConfig,
    TAccountTreasury,
    TAccountRandomnessAccount,
    TAccountCaller
  >,
  config?: { programAddress?: TProgramAddress },
): SettleDrawPublicInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountWinningBatch,
  TAccountWinner,
  TAccountConfig,
  TAccountTreasury,
  TAccountRandomnessAccount,
  TAccountCaller
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    winningBatch: { value: input.winningBatch ?? null, isWritable: false },
    winner: { value: input.winner ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    treasury: { value: input.treasury ?? null, isWritable: true },
    randomnessAccount: {
      value: input.randomnessAccount ?? null,
      isWritable: false,
    },
    caller: { value: input.caller ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("winningBatch", accounts.winningBatch),
      getAccountMeta("winner", accounts.winner),
      getAccountMeta("config", accounts.config),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("randomnessAccount", accounts.randomnessAccount),
      getAccountMeta("caller", accounts.caller),
    ],
    data: getSettleDrawPublicInstructionDataEncoder().encode(
      args as SettleDrawPublicInstructionDataArgs,
    ),
    programAddress,
  } as SettleDrawPublicInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountWinningBatch,
    TAccountWinner,
    TAccountConfig,
    TAccountTreasury,
    TAccountRandomnessAccount,
    TAccountCaller
  >);
}

export type ParsedSettleDrawPublicInstruction<
  TProgram extends string = typeof RAFFLE_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
    /**
     * The TicketBatch claimed to contain the winning ticket. Must satisfy:
     * `batch.pool == pool.key()` and
     * `batch.first_ticket_id <= winner_id <= batch.last_ticket_id`.
     * Anchor's macro doesn't pre-derive the batch's PDA seed (we don't know
     * `first_ticket_id` here) — we validate the link manually in the
     * handler.
     */
    winningBatch: TAccountMetas[1];
    /**
     * The winner's wallet — must equal `winning_batch.owner`. mut to receive
     * 99.5% of the pot.
     */
    winner: TAccountMetas[2];
    config: TAccountMetas[3];
    /**
     * The treasury account from `ProtocolConfig.treasury`. mut to receive
     * `protocol_fee - vrf_paid`.
     */
    treasury: TAccountMetas[4];
    /**
     * The Switchboard On-Demand `RandomnessAccountData` PDA, must match the
     * one stored at commit time (`pool.vrf_request`).
     */
    randomnessAccount: TAccountMetas[5];
    /** Permissionless. Caller pays only the tx fee. */
    caller: TAccountMetas[6];
  };
  data: SettleDrawPublicInstructionData;
};

export function parseSettleDrawPublicInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSettleDrawPublicInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      winningBatch: getNextAccount(),
      winner: getNextAccount(),
      config: getNextAccount(),
      treasury: getNextAccount(),
      randomnessAccount: getNextAccount(),
      caller: getNextAccount(),
    },
    data: getSettleDrawPublicInstructionDataDecoder().decode(instruction.data),
  };
}
