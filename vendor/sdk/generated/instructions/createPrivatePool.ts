/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findPoolPda } from "../pdas";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";
import {
  getAccessModeDecoder,
  getAccessModeEncoder,
  type AccessMode,
  type AccessModeArgs,
} from "../types";

export const CREATE_PRIVATE_POOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([147, 167, 168, 211, 130, 220, 126, 59]);

export function getCreatePrivatePoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_PRIVATE_POOL_DISCRIMINATOR,
  );
}

export type CreatePrivatePoolInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountCreator extends string
        ? WritableSignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreatePrivatePoolInstructionData = {
  discriminator: ReadonlyUint8Array;
  poolId: bigint;
  ticketPrice: bigint;
  duration: bigint;
  creatorFeeBps: number;
  accessMode: AccessMode;
  merkleRoot: ReadonlyUint8Array;
};

export type CreatePrivatePoolInstructionDataArgs = {
  poolId: number | bigint;
  ticketPrice: number | bigint;
  duration: number | bigint;
  creatorFeeBps: number;
  accessMode: AccessModeArgs;
  merkleRoot: ReadonlyUint8Array;
};

export function getCreatePrivatePoolInstructionDataEncoder(): FixedSizeEncoder<CreatePrivatePoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["poolId", getU64Encoder()],
      ["ticketPrice", getU64Encoder()],
      ["duration", getI64Encoder()],
      ["creatorFeeBps", getU16Encoder()],
      ["accessMode", getAccessModeEncoder()],
      ["merkleRoot", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: CREATE_PRIVATE_POOL_DISCRIMINATOR }),
  );
}

export function getCreatePrivatePoolInstructionDataDecoder(): FixedSizeDecoder<CreatePrivatePoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["poolId", getU64Decoder()],
    ["ticketPrice", getU64Decoder()],
    ["duration", getI64Decoder()],
    ["creatorFeeBps", getU16Decoder()],
    ["accessMode", getAccessModeDecoder()],
    ["merkleRoot", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getCreatePrivatePoolInstructionDataCodec(): FixedSizeCodec<
  CreatePrivatePoolInstructionDataArgs,
  CreatePrivatePoolInstructionData
> {
  return combineCodec(
    getCreatePrivatePoolInstructionDataEncoder(),
    getCreatePrivatePoolInstructionDataDecoder(),
  );
}

export type CreatePrivatePoolAsyncInput<
  TAccountPool extends string = string,
  TAccountCreator extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * One pool per (creator, pool_id) tuple. Creator-supplied pool_id
   * (D-050); Anchor's `init` constraint catches collisions on the same
   * (creator, pool_id) combination.
   */
  pool?: Address<TAccountPool>;
  /**
   * Pays rent for the new pool PDA. Permissionless — any wallet can be a
   * creator (spec line 49).
   */
  creator: TransactionSigner<TAccountCreator>;
  systemProgram?: Address<TAccountSystemProgram>;
  poolId: CreatePrivatePoolInstructionDataArgs["poolId"];
  ticketPrice: CreatePrivatePoolInstructionDataArgs["ticketPrice"];
  duration: CreatePrivatePoolInstructionDataArgs["duration"];
  creatorFeeBps: CreatePrivatePoolInstructionDataArgs["creatorFeeBps"];
  accessMode: CreatePrivatePoolInstructionDataArgs["accessMode"];
  merkleRoot: CreatePrivatePoolInstructionDataArgs["merkleRoot"];
};

export async function getCreatePrivatePoolInstructionAsync<
  TAccountPool extends string,
  TAccountCreator extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: CreatePrivatePoolAsyncInput<
    TAccountPool,
    TAccountCreator,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreatePrivatePoolInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountCreator,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.pool.value) {
    accounts.pool.value = await findPoolPda({
      creator: getAddressFromResolvedInstructionAccount(
        "creator",
        accounts.creator.value,
      ),
      poolId: getNonNullResolvedInstructionInput("poolId", args.poolId),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreatePrivatePoolInstructionDataEncoder().encode(
      args as CreatePrivatePoolInstructionDataArgs,
    ),
    programAddress,
  } as CreatePrivatePoolInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountCreator,
    TAccountSystemProgram
  >);
}

export type CreatePrivatePoolInput<
  TAccountPool extends string = string,
  TAccountCreator extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * One pool per (creator, pool_id) tuple. Creator-supplied pool_id
   * (D-050); Anchor's `init` constraint catches collisions on the same
   * (creator, pool_id) combination.
   */
  pool: Address<TAccountPool>;
  /**
   * Pays rent for the new pool PDA. Permissionless — any wallet can be a
   * creator (spec line 49).
   */
  creator: TransactionSigner<TAccountCreator>;
  systemProgram?: Address<TAccountSystemProgram>;
  poolId: CreatePrivatePoolInstructionDataArgs["poolId"];
  ticketPrice: CreatePrivatePoolInstructionDataArgs["ticketPrice"];
  duration: CreatePrivatePoolInstructionDataArgs["duration"];
  creatorFeeBps: CreatePrivatePoolInstructionDataArgs["creatorFeeBps"];
  accessMode: CreatePrivatePoolInstructionDataArgs["accessMode"];
  merkleRoot: CreatePrivatePoolInstructionDataArgs["merkleRoot"];
};

export function getCreatePrivatePoolInstruction<
  TAccountPool extends string,
  TAccountCreator extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: CreatePrivatePoolInput<
    TAccountPool,
    TAccountCreator,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreatePrivatePoolInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountCreator,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreatePrivatePoolInstructionDataEncoder().encode(
      args as CreatePrivatePoolInstructionDataArgs,
    ),
    programAddress,
  } as CreatePrivatePoolInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountCreator,
    TAccountSystemProgram
  >);
}

export type ParsedCreatePrivatePoolInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * One pool per (creator, pool_id) tuple. Creator-supplied pool_id
     * (D-050); Anchor's `init` constraint catches collisions on the same
     * (creator, pool_id) combination.
     */
    pool: TAccountMetas[0];
    /**
     * Pays rent for the new pool PDA. Permissionless — any wallet can be a
     * creator (spec line 49).
     */
    creator: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CreatePrivatePoolInstructionData;
};

export function parseCreatePrivatePoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreatePrivatePoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      creator: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreatePrivatePoolInstructionDataDecoder().decode(instruction.data),
  };
}
