/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_PROTOCOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([188, 233, 252, 106, 134, 146, 202, 91]);

export function getInitializeProtocolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_PROTOCOL_DISCRIMINATOR,
  );
}

export type InitializeProtocolInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountDeployer extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> =
    "qWyk54XHmEaRhYCuuhoEPKSWRnucyiUiVJGZJFvZB1M",
  TAccountProgramData extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountDeployer extends string
        ? WritableSignerAccount<TAccountDeployer> &
            AccountSignerMeta<TAccountDeployer>
        : TAccountDeployer,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      TAccountProgramData extends string
        ? ReadonlyAccount<TAccountProgramData>
        : TAccountProgramData,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeProtocolInstructionData = {
  discriminator: ReadonlyUint8Array;
  treasury: Address;
  vrfOracle: Address;
};

export type InitializeProtocolInstructionDataArgs = {
  treasury: Address;
  vrfOracle: Address;
};

export function getInitializeProtocolInstructionDataEncoder(): FixedSizeEncoder<InitializeProtocolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["treasury", getAddressEncoder()],
      ["vrfOracle", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_PROTOCOL_DISCRIMINATOR }),
  );
}

export function getInitializeProtocolInstructionDataDecoder(): FixedSizeDecoder<InitializeProtocolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["treasury", getAddressDecoder()],
    ["vrfOracle", getAddressDecoder()],
  ]);
}

export function getInitializeProtocolInstructionDataCodec(): FixedSizeCodec<
  InitializeProtocolInstructionDataArgs,
  InitializeProtocolInstructionData
> {
  return combineCodec(
    getInitializeProtocolInstructionDataEncoder(),
    getInitializeProtocolInstructionDataDecoder(),
  );
}

export type InitializeProtocolAsyncInput<
  TAccountConfig extends string = string,
  TAccountDeployer extends string = string,
  TAccountProgram extends string = string,
  TAccountProgramData extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config?: Address<TAccountConfig>;
  /**
   * Must be the program's upgrade authority. Spec § Security requirements
   * — "no authority/admin keys with pool-mutation power". The deploy
   * authority's only on-chain power is calling this single instruction
   * once at deploy time; the `init` constraint prevents replay.
   */
  deployer: TransactionSigner<TAccountDeployer>;
  /**
   * The Raffle program's own account, used to derive its programdata
   * address and verify it matches `program_data` below.
   */
  program?: Address<TAccountProgram>;
  /**
   * The BPF-Upgradeable-Loader-owned ProgramData account. Anchor checks
   * the discriminator + ownership; we further constrain that its
   * `upgrade_authority_address` equals `deployer.key()`.
   */
  programData: Address<TAccountProgramData>;
  systemProgram?: Address<TAccountSystemProgram>;
  treasury: InitializeProtocolInstructionDataArgs["treasury"];
  vrfOracle: InitializeProtocolInstructionDataArgs["vrfOracle"];
};

export async function getInitializeProtocolInstructionAsync<
  TAccountConfig extends string,
  TAccountDeployer extends string,
  TAccountProgram extends string,
  TAccountProgramData extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: InitializeProtocolAsyncInput<
    TAccountConfig,
    TAccountDeployer,
    TAccountProgram,
    TAccountProgramData,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeProtocolInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountDeployer,
    TAccountProgram,
    TAccountProgramData,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    deployer: { value: input.deployer ?? null, isWritable: true },
    program: { value: input.program ?? null, isWritable: false },
    programData: { value: input.programData ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.program.value) {
    accounts.program.value =
      "qWyk54XHmEaRhYCuuhoEPKSWRnucyiUiVJGZJFvZB1M" as Address<"qWyk54XHmEaRhYCuuhoEPKSWRnucyiUiVJGZJFvZB1M">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("deployer", accounts.deployer),
      getAccountMeta("program", accounts.program),
      getAccountMeta("programData", accounts.programData),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeProtocolInstructionDataEncoder().encode(
      args as InitializeProtocolInstructionDataArgs,
    ),
    programAddress,
  } as InitializeProtocolInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountDeployer,
    TAccountProgram,
    TAccountProgramData,
    TAccountSystemProgram
  >);
}

export type InitializeProtocolInput<
  TAccountConfig extends string = string,
  TAccountDeployer extends string = string,
  TAccountProgram extends string = string,
  TAccountProgramData extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config: Address<TAccountConfig>;
  /**
   * Must be the program's upgrade authority. Spec § Security requirements
   * — "no authority/admin keys with pool-mutation power". The deploy
   * authority's only on-chain power is calling this single instruction
   * once at deploy time; the `init` constraint prevents replay.
   */
  deployer: TransactionSigner<TAccountDeployer>;
  /**
   * The Raffle program's own account, used to derive its programdata
   * address and verify it matches `program_data` below.
   */
  program?: Address<TAccountProgram>;
  /**
   * The BPF-Upgradeable-Loader-owned ProgramData account. Anchor checks
   * the discriminator + ownership; we further constrain that its
   * `upgrade_authority_address` equals `deployer.key()`.
   */
  programData: Address<TAccountProgramData>;
  systemProgram?: Address<TAccountSystemProgram>;
  treasury: InitializeProtocolInstructionDataArgs["treasury"];
  vrfOracle: InitializeProtocolInstructionDataArgs["vrfOracle"];
};

export function getInitializeProtocolInstruction<
  TAccountConfig extends string,
  TAccountDeployer extends string,
  TAccountProgram extends string,
  TAccountProgramData extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: InitializeProtocolInput<
    TAccountConfig,
    TAccountDeployer,
    TAccountProgram,
    TAccountProgramData,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeProtocolInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountDeployer,
  TAccountProgram,
  TAccountProgramData,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    deployer: { value: input.deployer ?? null, isWritable: true },
    program: { value: input.program ?? null, isWritable: false },
    programData: { value: input.programData ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.program.value) {
    accounts.program.value =
      "qWyk54XHmEaRhYCuuhoEPKSWRnucyiUiVJGZJFvZB1M" as Address<"qWyk54XHmEaRhYCuuhoEPKSWRnucyiUiVJGZJFvZB1M">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("deployer", accounts.deployer),
      getAccountMeta("program", accounts.program),
      getAccountMeta("programData", accounts.programData),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeProtocolInstructionDataEncoder().encode(
      args as InitializeProtocolInstructionDataArgs,
    ),
    programAddress,
  } as InitializeProtocolInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountDeployer,
    TAccountProgram,
    TAccountProgramData,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeProtocolInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    /**
     * Must be the program's upgrade authority. Spec § Security requirements
     * — "no authority/admin keys with pool-mutation power". The deploy
     * authority's only on-chain power is calling this single instruction
     * once at deploy time; the `init` constraint prevents replay.
     */
    deployer: TAccountMetas[1];
    /**
     * The Raffle program's own account, used to derive its programdata
     * address and verify it matches `program_data` below.
     */
    program: TAccountMetas[2];
    /**
     * The BPF-Upgradeable-Loader-owned ProgramData account. Anchor checks
     * the discriminator + ownership; we further constrain that its
     * `upgrade_authority_address` equals `deployer.key()`.
     */
    programData: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: InitializeProtocolInstructionData;
};

export function parseInitializeProtocolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeProtocolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      deployer: getNextAccount(),
      program: getNextAccount(),
      programData: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeProtocolInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
