/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { RAFFLE_V2_PROGRAM_ADDRESS } from "../programs";

export const COMMIT_DRAW_PRIVATE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([13, 74, 89, 138, 130, 159, 175, 147]);

export function getCommitDrawPrivateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    COMMIT_DRAW_PRIVATE_DISCRIMINATOR,
  );
}

export type CommitDrawPrivateInstruction<
  TProgram extends string = typeof RAFFLE_V2_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountRandomnessAccount extends string | AccountMeta<string> = string,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountRandomnessAccount extends string
        ? ReadonlyAccount<TAccountRandomnessAccount>
        : TAccountRandomnessAccount,
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CommitDrawPrivateInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CommitDrawPrivateInstructionDataArgs = {};

export function getCommitDrawPrivateInstructionDataEncoder(): FixedSizeEncoder<CommitDrawPrivateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: COMMIT_DRAW_PRIVATE_DISCRIMINATOR }),
  );
}

export function getCommitDrawPrivateInstructionDataDecoder(): FixedSizeDecoder<CommitDrawPrivateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCommitDrawPrivateInstructionDataCodec(): FixedSizeCodec<
  CommitDrawPrivateInstructionDataArgs,
  CommitDrawPrivateInstructionData
> {
  return combineCodec(
    getCommitDrawPrivateInstructionDataEncoder(),
    getCommitDrawPrivateInstructionDataDecoder(),
  );
}

export type CommitDrawPrivateInput<
  TAccountPool extends string = string,
  TAccountRandomnessAccount extends string = string,
  TAccountCaller extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pool: Address<TAccountPool>;
  /** the same raw-bytes parser as the public flow (D-035). */
  randomnessAccount: Address<TAccountRandomnessAccount>;
  /**
   * Permissionless. Refunded from `min(pool_fee, VRF_COST_LAMPORTS)` per
   * D-030 / D-036. Same shortfall-absorption rules as public.
   */
  caller: TransactionSigner<TAccountCaller>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCommitDrawPrivateInstruction<
  TAccountPool extends string,
  TAccountRandomnessAccount extends string,
  TAccountCaller extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_V2_PROGRAM_ADDRESS,
>(
  input: CommitDrawPrivateInput<
    TAccountPool,
    TAccountRandomnessAccount,
    TAccountCaller,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CommitDrawPrivateInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountRandomnessAccount,
  TAccountCaller,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    randomnessAccount: {
      value: input.randomnessAccount ?? null,
      isWritable: false,
    },
    caller: { value: input.caller ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("randomnessAccount", accounts.randomnessAccount),
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCommitDrawPrivateInstructionDataEncoder().encode({}),
    programAddress,
  } as CommitDrawPrivateInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountRandomnessAccount,
    TAccountCaller,
    TAccountSystemProgram
  >);
}

export type ParsedCommitDrawPrivateInstruction<
  TProgram extends string = typeof RAFFLE_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
    /** the same raw-bytes parser as the public flow (D-035). */
    randomnessAccount: TAccountMetas[1];
    /**
     * Permissionless. Refunded from `min(pool_fee, VRF_COST_LAMPORTS)` per
     * D-030 / D-036. Same shortfall-absorption rules as public.
     */
    caller: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CommitDrawPrivateInstructionData;
};

export function parseCommitDrawPrivateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCommitDrawPrivateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      randomnessAccount: getNextAccount(),
      caller: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCommitDrawPrivateInstructionDataDecoder().decode(instruction.data),
  };
}
