/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findWhitelistedPda } from "../pdas";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";

export const REDEEM_INVITE_CODE_WHITELIST_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([177, 18, 111, 137, 171, 4, 49, 154]);

export function getRedeemInviteCodeWhitelistDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REDEEM_INVITE_CODE_WHITELIST_DISCRIMINATOR,
  );
}

export type RedeemInviteCodeWhitelistInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountRedemptionRecord extends string | AccountMeta<string> = string,
  TAccountWhitelisted extends string | AccountMeta<string> = string,
  TAccountBuyer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? ReadonlyAccount<TAccountPool>
        : TAccountPool,
      TAccountRedemptionRecord extends string
        ? WritableAccount<TAccountRedemptionRecord>
        : TAccountRedemptionRecord,
      TAccountWhitelisted extends string
        ? WritableAccount<TAccountWhitelisted>
        : TAccountWhitelisted,
      TAccountBuyer extends string
        ? WritableSignerAccount<TAccountBuyer> &
            AccountSignerMeta<TAccountBuyer>
        : TAccountBuyer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RedeemInviteCodeWhitelistInstructionData = {
  discriminator: ReadonlyUint8Array;
  code: ReadonlyUint8Array;
  proof: Array<ReadonlyUint8Array>;
};

export type RedeemInviteCodeWhitelistInstructionDataArgs = {
  code: ReadonlyUint8Array;
  proof: Array<ReadonlyUint8Array>;
};

export function getRedeemInviteCodeWhitelistInstructionDataEncoder(): Encoder<RedeemInviteCodeWhitelistInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["code", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
      ["proof", getArrayEncoder(fixEncoderSize(getBytesEncoder(), 32))],
    ]),
    (value) => ({
      ...value,
      discriminator: REDEEM_INVITE_CODE_WHITELIST_DISCRIMINATOR,
    }),
  );
}

export function getRedeemInviteCodeWhitelistInstructionDataDecoder(): Decoder<RedeemInviteCodeWhitelistInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["code", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
    ["proof", getArrayDecoder(fixDecoderSize(getBytesDecoder(), 32))],
  ]);
}

export function getRedeemInviteCodeWhitelistInstructionDataCodec(): Codec<
  RedeemInviteCodeWhitelistInstructionDataArgs,
  RedeemInviteCodeWhitelistInstructionData
> {
  return combineCodec(
    getRedeemInviteCodeWhitelistInstructionDataEncoder(),
    getRedeemInviteCodeWhitelistInstructionDataDecoder(),
  );
}

export type RedeemInviteCodeWhitelistAsyncInput<
  TAccountPool extends string = string,
  TAccountRedemptionRecord extends string = string,
  TAccountWhitelisted extends string = string,
  TAccountBuyer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pool: Address<TAccountPool>;
  /**
   * Marker PDA proving the code is consumed. `init` here is the replay
   * guard: a second redemption with the same code hash collides on this
   * PDA's address and Anchor rejects.
   */
  redemptionRecord: Address<TAccountRedemptionRecord>;
  /**
   * Whitelist marker — eligibility for `buy_ticket_private`. Per (pool,
   * wallet); a wallet can have at most one whitelist entry per pool.
   */
  whitelisted?: Address<TAccountWhitelisted>;
  buyer: TransactionSigner<TAccountBuyer>;
  systemProgram?: Address<TAccountSystemProgram>;
  code: RedeemInviteCodeWhitelistInstructionDataArgs["code"];
  proof: RedeemInviteCodeWhitelistInstructionDataArgs["proof"];
};

export async function getRedeemInviteCodeWhitelistInstructionAsync<
  TAccountPool extends string,
  TAccountRedemptionRecord extends string,
  TAccountWhitelisted extends string,
  TAccountBuyer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: RedeemInviteCodeWhitelistAsyncInput<
    TAccountPool,
    TAccountRedemptionRecord,
    TAccountWhitelisted,
    TAccountBuyer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RedeemInviteCodeWhitelistInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountRedemptionRecord,
    TAccountWhitelisted,
    TAccountBuyer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: false },
    redemptionRecord: {
      value: input.redemptionRecord ?? null,
      isWritable: true,
    },
    whitelisted: { value: input.whitelisted ?? null, isWritable: true },
    buyer: { value: input.buyer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.whitelisted.value) {
    accounts.whitelisted.value = await findWhitelistedPda({
      pool: getAddressFromResolvedInstructionAccount(
        "pool",
        accounts.pool.value,
      ),
      buyer: getAddressFromResolvedInstructionAccount(
        "buyer",
        accounts.buyer.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("redemptionRecord", accounts.redemptionRecord),
      getAccountMeta("whitelisted", accounts.whitelisted),
      getAccountMeta("buyer", accounts.buyer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRedeemInviteCodeWhitelistInstructionDataEncoder().encode(
      args as RedeemInviteCodeWhitelistInstructionDataArgs,
    ),
    programAddress,
  } as RedeemInviteCodeWhitelistInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountRedemptionRecord,
    TAccountWhitelisted,
    TAccountBuyer,
    TAccountSystemProgram
  >);
}

export type RedeemInviteCodeWhitelistInput<
  TAccountPool extends string = string,
  TAccountRedemptionRecord extends string = string,
  TAccountWhitelisted extends string = string,
  TAccountBuyer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pool: Address<TAccountPool>;
  /**
   * Marker PDA proving the code is consumed. `init` here is the replay
   * guard: a second redemption with the same code hash collides on this
   * PDA's address and Anchor rejects.
   */
  redemptionRecord: Address<TAccountRedemptionRecord>;
  /**
   * Whitelist marker — eligibility for `buy_ticket_private`. Per (pool,
   * wallet); a wallet can have at most one whitelist entry per pool.
   */
  whitelisted: Address<TAccountWhitelisted>;
  buyer: TransactionSigner<TAccountBuyer>;
  systemProgram?: Address<TAccountSystemProgram>;
  code: RedeemInviteCodeWhitelistInstructionDataArgs["code"];
  proof: RedeemInviteCodeWhitelistInstructionDataArgs["proof"];
};

export function getRedeemInviteCodeWhitelistInstruction<
  TAccountPool extends string,
  TAccountRedemptionRecord extends string,
  TAccountWhitelisted extends string,
  TAccountBuyer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: RedeemInviteCodeWhitelistInput<
    TAccountPool,
    TAccountRedemptionRecord,
    TAccountWhitelisted,
    TAccountBuyer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RedeemInviteCodeWhitelistInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountRedemptionRecord,
  TAccountWhitelisted,
  TAccountBuyer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: false },
    redemptionRecord: {
      value: input.redemptionRecord ?? null,
      isWritable: true,
    },
    whitelisted: { value: input.whitelisted ?? null, isWritable: true },
    buyer: { value: input.buyer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("redemptionRecord", accounts.redemptionRecord),
      getAccountMeta("whitelisted", accounts.whitelisted),
      getAccountMeta("buyer", accounts.buyer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRedeemInviteCodeWhitelistInstructionDataEncoder().encode(
      args as RedeemInviteCodeWhitelistInstructionDataArgs,
    ),
    programAddress,
  } as RedeemInviteCodeWhitelistInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountRedemptionRecord,
    TAccountWhitelisted,
    TAccountBuyer,
    TAccountSystemProgram
  >);
}

export type ParsedRedeemInviteCodeWhitelistInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
    /**
     * Marker PDA proving the code is consumed. `init` here is the replay
     * guard: a second redemption with the same code hash collides on this
     * PDA's address and Anchor rejects.
     */
    redemptionRecord: TAccountMetas[1];
    /**
     * Whitelist marker — eligibility for `buy_ticket_private`. Per (pool,
     * wallet); a wallet can have at most one whitelist entry per pool.
     */
    whitelisted: TAccountMetas[2];
    buyer: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: RedeemInviteCodeWhitelistInstructionData;
};

export function parseRedeemInviteCodeWhitelistInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRedeemInviteCodeWhitelistInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      redemptionRecord: getNextAccount(),
      whitelisted: getNextAccount(),
      buyer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRedeemInviteCodeWhitelistInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
