/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_EMPTY_PRIVATE_POOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([46, 238, 179, 33, 131, 162, 98, 73]);

export function getCloseEmptyPrivatePoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_EMPTY_PRIVATE_POOL_DISCRIMINATOR,
  );
}

export type CloseEmptyPrivatePoolInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountCreator extends string
        ? WritableSignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      ...TRemainingAccounts,
    ]
  >;

export type CloseEmptyPrivatePoolInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseEmptyPrivatePoolInstructionDataArgs = {};

export function getCloseEmptyPrivatePoolInstructionDataEncoder(): FixedSizeEncoder<CloseEmptyPrivatePoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLOSE_EMPTY_PRIVATE_POOL_DISCRIMINATOR,
    }),
  );
}

export function getCloseEmptyPrivatePoolInstructionDataDecoder(): FixedSizeDecoder<CloseEmptyPrivatePoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseEmptyPrivatePoolInstructionDataCodec(): FixedSizeCodec<
  CloseEmptyPrivatePoolInstructionDataArgs,
  CloseEmptyPrivatePoolInstructionData
> {
  return combineCodec(
    getCloseEmptyPrivatePoolInstructionDataEncoder(),
    getCloseEmptyPrivatePoolInstructionDataDecoder(),
  );
}

export type CloseEmptyPrivatePoolInput<
  TAccountPool extends string = string,
  TAccountCreator extends string = string,
> = {
  /**
   * Anchor's `close = creator` constraint zeroes the account and refunds
   * rent to the creator. Constraint also enforces `total_tickets == 0`.
   */
  pool: Address<TAccountPool>;
  /**
   * Only the original creator can claim the rent. `mut` so the system
   * program can credit lamports back.
   */
  creator: TransactionSigner<TAccountCreator>;
};

export function getCloseEmptyPrivatePoolInstruction<
  TAccountPool extends string,
  TAccountCreator extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: CloseEmptyPrivatePoolInput<TAccountPool, TAccountCreator>,
  config?: { programAddress?: TProgramAddress },
): CloseEmptyPrivatePoolInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountCreator
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("creator", accounts.creator),
    ],
    data: getCloseEmptyPrivatePoolInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseEmptyPrivatePoolInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountCreator
  >);
}

export type ParsedCloseEmptyPrivatePoolInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Anchor's `close = creator` constraint zeroes the account and refunds
     * rent to the creator. Constraint also enforces `total_tickets == 0`.
     */
    pool: TAccountMetas[0];
    /**
     * Only the original creator can claim the rent. `mut` so the system
     * program can credit lamports back.
     */
    creator: TAccountMetas[1];
  };
  data: CloseEmptyPrivatePoolInstructionData;
};

export function parseCloseEmptyPrivatePoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseEmptyPrivatePoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { pool: getNextAccount(), creator: getNextAccount() },
    data: getCloseEmptyPrivatePoolInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
