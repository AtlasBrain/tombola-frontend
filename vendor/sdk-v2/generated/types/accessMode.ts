/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getEnumDecoder,
  getEnumEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Two access modes for private pools (spec lines 54, 87–89):
 * - `WhitelistMode`: redeeming a code marks the buyer's wallet as
 * whitelisted; they can then buy unlimited tickets in this pool.
 * - `OneCodePerTicket`: each code redemption mints exactly one ticket; the
 * code is burned. The buyer never has a separate "whitelisted" PDA.
 */
export enum AccessMode {
  WhitelistMode,
  OneCodePerTicket,
}

export type AccessModeArgs = AccessMode;

export function getAccessModeEncoder(): FixedSizeEncoder<AccessModeArgs> {
  return getEnumEncoder(AccessMode);
}

export function getAccessModeDecoder(): FixedSizeDecoder<AccessMode> {
  return getEnumDecoder(AccessMode);
}

export function getAccessModeCodec(): FixedSizeCodec<
  AccessModeArgs,
  AccessMode
> {
  return combineCodec(getAccessModeEncoder(), getAccessModeDecoder());
}
