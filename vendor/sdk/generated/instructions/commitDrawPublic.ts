/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { RAFFLE_PROGRAM_ADDRESS } from "../programs";

export const COMMIT_DRAW_PUBLIC_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([181, 222, 188, 113, 116, 15, 178, 180]);

export function getCommitDrawPublicDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    COMMIT_DRAW_PUBLIC_DISCRIMINATOR,
  );
}

export type CommitDrawPublicInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountRandomnessAccount extends string | AccountMeta<string> = string,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountRandomnessAccount extends string
        ? ReadonlyAccount<TAccountRandomnessAccount>
        : TAccountRandomnessAccount,
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CommitDrawPublicInstructionData = {
  discriminator: ReadonlyUint8Array;
  poolType: number;
  round: bigint;
};

export type CommitDrawPublicInstructionDataArgs = {
  poolType: number;
  round: number | bigint;
};

export function getCommitDrawPublicInstructionDataEncoder(): FixedSizeEncoder<CommitDrawPublicInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["poolType", getU8Encoder()],
      ["round", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: COMMIT_DRAW_PUBLIC_DISCRIMINATOR }),
  );
}

export function getCommitDrawPublicInstructionDataDecoder(): FixedSizeDecoder<CommitDrawPublicInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["poolType", getU8Decoder()],
    ["round", getU64Decoder()],
  ]);
}

export function getCommitDrawPublicInstructionDataCodec(): FixedSizeCodec<
  CommitDrawPublicInstructionDataArgs,
  CommitDrawPublicInstructionData
> {
  return combineCodec(
    getCommitDrawPublicInstructionDataEncoder(),
    getCommitDrawPublicInstructionDataDecoder(),
  );
}

export type CommitDrawPublicInput<
  TAccountPool extends string = string,
  TAccountRandomnessAccount extends string = string,
  TAccountCaller extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pool: Address<TAccountPool>;
  /**
   * The Switchboard On-Demand `RandomnessAccountData` PDA. We only store
   * its key here — Switchboard's own `commitIx` (bundled by the client in
   * the same tx) is what initialises the account's seed_slot. Validation
   * of the account's data happens at settle time, not commit.
   * crate at runtime (D-035). We parse via raw bytes in `crate::vrf` at
   * settle time.
   */
  randomnessAccount: Address<TAccountRandomnessAccount>;
  /**
   * Permissionless. The caller pays Switchboard's commit fee out-of-pocket
   * in the bundled tx; this handler refunds them up to `VRF_COST_LAMPORTS`
   * from the pool's accumulated protocol fee (D-030).
   */
  caller: TransactionSigner<TAccountCaller>;
  systemProgram?: Address<TAccountSystemProgram>;
  poolType: CommitDrawPublicInstructionDataArgs["poolType"];
  round: CommitDrawPublicInstructionDataArgs["round"];
};

export function getCommitDrawPublicInstruction<
  TAccountPool extends string,
  TAccountRandomnessAccount extends string,
  TAccountCaller extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_PROGRAM_ADDRESS,
>(
  input: CommitDrawPublicInput<
    TAccountPool,
    TAccountRandomnessAccount,
    TAccountCaller,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CommitDrawPublicInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountRandomnessAccount,
  TAccountCaller,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    randomnessAccount: {
      value: input.randomnessAccount ?? null,
      isWritable: false,
    },
    caller: { value: input.caller ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("randomnessAccount", accounts.randomnessAccount),
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCommitDrawPublicInstructionDataEncoder().encode(
      args as CommitDrawPublicInstructionDataArgs,
    ),
    programAddress,
  } as CommitDrawPublicInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountRandomnessAccount,
    TAccountCaller,
    TAccountSystemProgram
  >);
}

export type ParsedCommitDrawPublicInstruction<
  TProgram extends string = typeof RAFFLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
    /**
     * The Switchboard On-Demand `RandomnessAccountData` PDA. We only store
     * its key here — Switchboard's own `commitIx` (bundled by the client in
     * the same tx) is what initialises the account's seed_slot. Validation
     * of the account's data happens at settle time, not commit.
     * crate at runtime (D-035). We parse via raw bytes in `crate::vrf` at
     * settle time.
     */
    randomnessAccount: TAccountMetas[1];
    /**
     * Permissionless. The caller pays Switchboard's commit fee out-of-pocket
     * in the bundled tx; this handler refunds them up to `VRF_COST_LAMPORTS`
     * from the pool's accumulated protocol fee (D-030).
     */
    caller: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CommitDrawPublicInstructionData;
};

export function parseCommitDrawPublicInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCommitDrawPublicInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      randomnessAccount: getNextAccount(),
      caller: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCommitDrawPublicInstructionDataDecoder().decode(instruction.data),
  };
}
