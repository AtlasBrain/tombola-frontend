/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { RAFFLE_V2_PROGRAM_ADDRESS } from "../programs";

export const BUY_TICKET_PUBLIC_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([49, 138, 246, 224, 59, 60, 21, 151]);

export function getBuyTicketPublicDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    BUY_TICKET_PUBLIC_DISCRIMINATOR,
  );
}

export type BuyTicketPublicInstruction<
  TProgram extends string = typeof RAFFLE_V2_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountTicketBatch extends string | AccountMeta<string> = string,
  TAccountBuyer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountTicketBatch extends string
        ? WritableAccount<TAccountTicketBatch>
        : TAccountTicketBatch,
      TAccountBuyer extends string
        ? WritableSignerAccount<TAccountBuyer> &
            AccountSignerMeta<TAccountBuyer>
        : TAccountBuyer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type BuyTicketPublicInstructionData = {
  discriminator: ReadonlyUint8Array;
  poolType: number;
  round: bigint;
  quantity: bigint;
};

export type BuyTicketPublicInstructionDataArgs = {
  poolType: number;
  round: number | bigint;
  quantity: number | bigint;
};

export function getBuyTicketPublicInstructionDataEncoder(): FixedSizeEncoder<BuyTicketPublicInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["poolType", getU8Encoder()],
      ["round", getU64Encoder()],
      ["quantity", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BUY_TICKET_PUBLIC_DISCRIMINATOR }),
  );
}

export function getBuyTicketPublicInstructionDataDecoder(): FixedSizeDecoder<BuyTicketPublicInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["poolType", getU8Decoder()],
    ["round", getU64Decoder()],
    ["quantity", getU64Decoder()],
  ]);
}

export function getBuyTicketPublicInstructionDataCodec(): FixedSizeCodec<
  BuyTicketPublicInstructionDataArgs,
  BuyTicketPublicInstructionData
> {
  return combineCodec(
    getBuyTicketPublicInstructionDataEncoder(),
    getBuyTicketPublicInstructionDataDecoder(),
  );
}

export type BuyTicketPublicInput<
  TAccountPool extends string = string,
  TAccountTicketBatch extends string = string,
  TAccountBuyer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pool: Address<TAccountPool>;
  /**
   * One `TicketBatch` PDA per *purchase*. Seeded by the pool key + the
   * first_ticket_id of this purchase (which equals `pool.total_tickets` at
   * instruction-entry time). Two concurrent buyers will both derive the
   * same PDA address → one tx wins, the other fails account-already-exists.
   * The losing client retries with a refreshed `total_tickets`.
   */
  ticketBatch: Address<TAccountTicketBatch>;
  buyer: TransactionSigner<TAccountBuyer>;
  systemProgram?: Address<TAccountSystemProgram>;
  poolType: BuyTicketPublicInstructionDataArgs["poolType"];
  round: BuyTicketPublicInstructionDataArgs["round"];
  quantity: BuyTicketPublicInstructionDataArgs["quantity"];
};

export function getBuyTicketPublicInstruction<
  TAccountPool extends string,
  TAccountTicketBatch extends string,
  TAccountBuyer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFLE_V2_PROGRAM_ADDRESS,
>(
  input: BuyTicketPublicInput<
    TAccountPool,
    TAccountTicketBatch,
    TAccountBuyer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): BuyTicketPublicInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountTicketBatch,
  TAccountBuyer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFLE_V2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    ticketBatch: { value: input.ticketBatch ?? null, isWritable: true },
    buyer: { value: input.buyer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("ticketBatch", accounts.ticketBatch),
      getAccountMeta("buyer", accounts.buyer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getBuyTicketPublicInstructionDataEncoder().encode(
      args as BuyTicketPublicInstructionDataArgs,
    ),
    programAddress,
  } as BuyTicketPublicInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountTicketBatch,
    TAccountBuyer,
    TAccountSystemProgram
  >);
}

export type ParsedBuyTicketPublicInstruction<
  TProgram extends string = typeof RAFFLE_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
    /**
     * One `TicketBatch` PDA per *purchase*. Seeded by the pool key + the
     * first_ticket_id of this purchase (which equals `pool.total_tickets` at
     * instruction-entry time). Two concurrent buyers will both derive the
     * same PDA address → one tx wins, the other fails account-already-exists.
     * The losing client retries with a refreshed `total_tickets`.
     */
    ticketBatch: TAccountMetas[1];
    buyer: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: BuyTicketPublicInstructionData;
};

export function parseBuyTicketPublicInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedBuyTicketPublicInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      ticketBatch: getNextAccount(),
      buyer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getBuyTicketPublicInstructionDataDecoder().decode(instruction.data),
  };
}
